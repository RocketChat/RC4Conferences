export const verifyAdmin = () => {
  const callVerify = () => {};
  return [callVerify, { data: null, error: null, loading: false }];
};

export const ssrVerifyAdmin = async () => false;

export const ssrVerifySpeaker = async () => false;
